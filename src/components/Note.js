const Note = ({title, onAdd, showAdd}) => {


        return (
            <header className='header'>
               <h5>Double click to show "in progress" status with greens sidebar</h5> 
            </header>
        )
        }

export default Note
